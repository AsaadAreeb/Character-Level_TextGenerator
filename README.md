# Character-Level_TextGenerator

Recurrent Neural Network (RNN) to generate text character by character. The project's core is the RNN model, trained to mimic the patterns present in the provided text.
